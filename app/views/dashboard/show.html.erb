<% content_for(:title, "Dashboard — Ruby Embassy") %>

<section class="pb-32">
  <div class="max-w-4xl mx-auto px-8 py-10">
    <% if flash[:notice] %>
      <div class="alert alert-notice"><%= flash[:notice] %></div>
    <% end %>

    <h1 class="text-hero leading-tight font-medium text-navy mb-4">
      Welcome<% if current_user.first_name.present? %>, <%= current_user.first_name %><% end %>
    </h1>
    <p class="text-lg text-gray mb-12">
      What would you like to do?
    </p>

    <% if (current_user.volunteer? || current_user.admin?) && @empty_volunteer_count.to_i > 0 %>
      <%= link_to schedule_path(kind: :volunteer), class: "action-card action-card--hero mb-6" do %>
        <h2 class="action-card__title">Volunteers Needed</h2>
        <p class="action-card__description">
          We need help with <%= pluralize(@empty_volunteer_count, "thing") %>.
          Pick a slot whenever you've got time.
        </p>
        <span class="action-card__arrow" aria-hidden="true">&rarr;</span>
      <% end %>
    <% end %>

    <div class="action-grid">
      <%= link_to schedule_path, class: "action-card" do %>
        <h2 class="action-card__title">See the schedule</h2>
        <p class="action-card__description">Browse all three days of talks, activities, and Ruby Embassy blocks.</p>
        <span class="action-card__arrow" aria-hidden="true">&rarr;</span>
      <% end %>

      <%= link_to plan_path, class: "action-card" do %>
        <h2 class="action-card__title">Plan your trip</h2>
        <p class="action-card__description">Save sessions, pick meal spots, and check your Embassy appointment.</p>
        <span class="action-card__arrow" aria-hidden="true">&rarr;</span>
      <% end %>

      <%= link_to new_schedule_item_path(schedule_item: { is_public: true }), class: "action-card" do %>
        <h2 class="action-card__title">Host an Activity</h2>
        <p class="action-card__description">Lead a meetup, group hike, or informal gathering.</p>
        <span class="action-card__arrow" aria-hidden="true">&rarr;</span>
      <% end %>

      <%= link_to schedule_path(kind: :embassy), class: "action-card" do %>
        <h2 class="action-card__title">Book a Ruby Embassy Appointment</h2>
        <p class="action-card__description">Pick a time block to stop by the Ruby Embassy.</p>
        <span class="action-card__arrow" aria-hidden="true">&rarr;</span>
      <% end %>

      <%#
      <a href="https://ashevillagers.blueridgeruby.com" class="action-card" target="_blank" rel="noopener">
        <h2 class="action-card__title">Get your Ashevillager</h2>
        <p class="action-card__description">Make your Ashevillager and join the locals you might run into around town.</p>
        <span class="action-card__arrow" aria-hidden="true">&#8599;</span>
      </a>
      %>

      <a href="https://rockymtnruby.dev/boulder-guide/" class="action-card" target="_blank" rel="noopener">
        <h2 class="action-card__title">Explore Boulder</h2>
        <p class="action-card__description">Local picks for food, coffee, hikes, and things to do off-schedule.</p>
        <span class="action-card__arrow" aria-hidden="true">&#8599;</span>
      </a>
    </div>
  </div>
</section>
